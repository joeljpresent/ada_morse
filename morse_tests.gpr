project Morse_Tests is

        for Languages use ("Ada");
        for Main use ("main_tests.adb");
        for Source_Dirs use ("src", "tests");
        for Object_Dir use "obj_tests";
        for Target use "x86_64-linux";

end Morse_Tests;
